apiVersion: v1
kind: Service
metadata:
  name: platform-copilot
  labels:
    app.kubernetes.io/name: platform-copilot
    app.kubernetes.io/part-of: ai-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: platform-copilot
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
